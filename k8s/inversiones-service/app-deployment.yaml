apiVersion: apps/v1
kind: Deployment
metadata:
  name: inversiones-service
  namespace: securebankito
  labels:
    app: inversiones-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inversiones-service
  template:
    metadata:
      labels:
        app: inversiones-service
    spec:
      containers:
        - name: inversiones-service
          image: securebankito/inversiones-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: inversiones-config
                  key: PORT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: inversiones-secret
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: inversiones-secret
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: inversiones-service
  namespace: securebankito
spec:
  selector:
    app: inversiones-service
  ports:
    - port: 3003
      targetPort: 3003
  type: ClusterIP
