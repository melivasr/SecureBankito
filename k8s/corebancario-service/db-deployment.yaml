apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-postgres
  namespace: securebankito
  labels:
    app: core-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core-postgres
  template:
    metadata:
      labels:
        app: core-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: corebancario-config
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: corebancario-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: corebancario-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: core-postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "core_user", "-d", "corebancario_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
      volumes:
        - name: core-postgres-data
          persistentVolumeClaim:
            claimName: core-postgres-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: core-postgres-data
  namespace: securebankito
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: core-postgres
  namespace: securebankito
spec:
  selector:
    app: core-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
